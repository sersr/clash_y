# Run with `dart run ffigen --config ffigen.yaml`.
name: ClashCoreBindings
description: |
  Bindings for `src/clash_core.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/clash_core_bindings_generated.dart'
headers:
  entry-points:
    - 'src/clash_core.h'
  include-directives:
    - 'src/clash_core.h'
ffi-native:
comments:
  style: any
  length: full
