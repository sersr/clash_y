# The Flutter tooling requires that developers have CMake 3.10 or later
# installed. You should not increase this version, as doing so will cause
# the plugin to fail to compile for some customers of the plugin.
cmake_minimum_required(VERSION 3.10)

project(clash_service_drawin_library VERSION 0.0.1 LANGUAGES C)

add_library(clash_service_drawin SHARED
  "clash_service_drawin.c"
)

set_target_properties(clash_service_drawin PROPERTIES
  PUBLIC_HEADER clash_service_drawin.h
  OUTPUT_NAME "clash_service_drawin"
)

target_compile_definitions(clash_service_drawin PUBLIC DART_SHARED_LIB)

if (ANDROID)
  # Support Android 15 16k page size
  target_link_options(clash_service_drawin PRIVATE "-Wl,-z,max-page-size=16384")
endif()
