# Run with `dart run ffigen --config ffigen.yaml`.
name: ClashServiceDrawinBindings
description: |
  Bindings for `src/clash_service_drawin.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/clash_service_drawin_bindings_generated.dart'
headers:
  entry-points:
    - 'src/clash_service_drawin.h'
  include-directives:
    - 'src/clash_service_drawin.h'
comments:
  style: any
  length: full
